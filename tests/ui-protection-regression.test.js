/**
 * UI protection regression tests.
 * Run by the pre-commit hook to catch behavioral regressions before commit.
 */

import { describe, it } from "node:test";
import assert from "node:assert/strict";

describe("UI protection stubs", () => {
  it("placeholder — add real regression tests here", () => {
    assert.ok(true);
  });
});
